// Pipeline state persistence is now part of `pipeline::PipelineStateFile` in `mod.rs`.
// This module is kept as a re-export point for backward compatibility.
pub use crate::pipeline::PipelineStateFile;
